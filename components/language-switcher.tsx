"use client"

interface LanguageSwitcherProps {
  className?: string
}

export default function LanguageSwitcher({ className }: LanguageSwitcherProps) {
  // Since we're only using English, we'll keep this component simple
  return null
}

